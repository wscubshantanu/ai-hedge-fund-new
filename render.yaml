services:
  - type: web
    name: ai-hedge-fund
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8000
      - key: DEMO_MODE
        value: "true"
      - key: SECRET_KEY
        generateValue: true
